[package]
name = "slint-bangumi"
version = "0.1.0"
edition = "2024"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
slint = "1"
thiserror = "2"
toml = "0.8"
walkdir = "2"

[build-dependencies]
slint-build = "1"
